import { WorkerAddon as WorkerAddonProps } from "@watchedcom/schema/dist/entities";

export interface WorkerAddon {}

export const createWorkerAddon = (
    props: Partial<WorkerAddonProps>
): WorkerAddon => {};
