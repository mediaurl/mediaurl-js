{
  "name": "@mediaurl/test-utils",
  "description": "Test utils for MediaURL addons",
  "version": "1.0.0-alpha.2",
  "main": "dist",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "prepublishOnly": "rm -rf dist && npm run build",
    "develop-addon": "ts-node-dev addon"
  },
  "dependencies": {
    "@mediaurl/sdk": "^1.0.0-alpha.2",
    "jest": "^26.4.2"
  },
  "devDependencies": {
    "supertest": "^6.0.1",
    "ts-jest": "^26.4.4",
    "ts-node-dev": "^1.1.1",
    "typescript": "^4.1.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
